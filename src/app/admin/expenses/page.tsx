import { createExpenseStatementEntry } from "@/actions/ledger.actions";
import { prisma } from "@/lib/prisma";
import { Table } from "../sales/page";

const fields = ["date", "productName", "cost", "quantity", "totalCost", "deliCharge", "closingAmount"];
const cols = ["Date", "Product Name", "Cost", "Quantity", "Total Cost", "Deli Charge", "Closing Amount"];
export default async function ExpensesPage() { const entries = await prisma.expenseStatementEntry.findMany({ orderBy: { date: "desc" }, take: 100 }); return <div className="space-y-6"><h1 className="text-2xl font-bold text-gray-900 dark:text-white">Expense Statement</h1><form action={createExpenseStatementEntry} className="grid gap-3 rounded-xl border border-gray-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900 md:grid-cols-3">{fields.map((field) => <input key={field} name={field} required={field === "date" || field === "productName"} type={field === "date" ? "date" : ["cost","quantity","totalCost","deliCharge","closingAmount"].includes(field) ? "number" : "text"} placeholder={cols[fields.indexOf(field)]} className="rounded-lg border border-gray-300 bg-white px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800" />)}<button className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Add Entry</button></form><Table cols={cols} rows={entries.map(x => [x.date.toLocaleDateString(),x.productName,x.cost,x.quantity,x.totalCost,x.deliCharge,x.closingAmount])} /></div>; }
