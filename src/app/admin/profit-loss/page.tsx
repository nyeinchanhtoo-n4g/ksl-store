import { prisma } from "@/lib/prisma";
import { Table } from "../sales/page";
import Link from "next/link";

export default async function ProfitLossPage() { const entries = await prisma.profitLossSummary.findMany({ orderBy: { startDate: "desc" }, take: 100 }); return <div className="space-y-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="text-2xl font-bold text-gray-900 dark:text-white">Manual Profit & Loss</h1><p className="mt-1 text-sm text-gray-500 dark:text-zinc-400">Values are entered manually and are not calculated from ledger rows.</p></div><Link href="/admin/profit-loss/new" className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">Create Profit & Loss</Link></div><Table cols={["Period","Start","End","Sales","Expense","Profit / Loss","Note"]} rows={entries.map(x => [x.period,x.startDate.toLocaleDateString(),x.endDate.toLocaleDateString(),x.salesAmount,x.expenseAmount,x.profitLoss,x.note])}/></div>; }
