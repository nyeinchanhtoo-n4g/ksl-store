import { createProfitLossSummary } from "@/actions/ledger.actions";
import { prisma } from "@/lib/prisma";
import SubmitButton from "@/components/admin/SubmitButton";
import { Table } from "../sales/page";

export default async function ProfitLossPage() { const entries = await prisma.profitLossSummary.findMany({ orderBy: { startDate: "desc" }, take: 100 }); return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-gray-900 dark:text-white">Manual Profit & Loss</h1><p className="mt-1 text-sm text-gray-500 dark:text-zinc-400">Values are entered manually and are not calculated from ledger rows.</p></div><form action={createProfitLossSummary} className="grid gap-3 rounded-xl border border-gray-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900 md:grid-cols-3"><select name="period" className="rounded-lg border border-gray-300 bg-white px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"><option value="DAILY">Daily</option><option value="WEEKLY">Weekly</option><option value="MONTHLY">Monthly</option><option value="YEARLY">Yearly</option></select><input name="startDate" required type="date" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input name="endDate" required type="date" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input name="salesAmount" type="text" inputMode="numeric" placeholder="Manual sales amount" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input name="expenseAmount" type="text" inputMode="numeric" placeholder="Manual expense amount" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input name="profitLoss" type="text" inputMode="numeric" placeholder="Manual profit / loss" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><input name="note" placeholder="Note" className="rounded-lg border border-gray-300 px-3 py-2 text-sm dark:border-zinc-700 dark:bg-zinc-800"/><SubmitButton pendingLabel="Saving..." className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Save Summary</SubmitButton></form><Table cols={["Period","Start","End","Sales","Expense","Profit / Loss","Note"]} rows={entries.map(x => [x.period,x.startDate.toLocaleDateString(),x.endDate.toLocaleDateString(),x.salesAmount,x.expenseAmount,x.profitLoss,x.note])}/></div>; }
